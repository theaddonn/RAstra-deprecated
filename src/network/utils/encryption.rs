use crate::network::conn_info::ConnInfo;

pub fn encrypt(data: Vec<u8>, conn_info: &mut ConnInfo) -> Vec<u8> {
    return data;
}

pub fn decrypt(data: Vec<u8>, conn_info: &ConnInfo) -> Vec<u8> {
    return data;
}

//TODO ADD ENCRYPTION SUPPORT!
